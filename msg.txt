Windows PC Collections page rebuilt to the RISE reference

The page is the programme's reference build, the one handed to every retailer
implementing Collections, not a page Novi invented. So it has to match rather
than be reworded into Novi's voice, which is what the first version did.

Reproduced exactly: the H1, the five sections in programme order with the
programme's own descriptors and a Shop all link, and the comparison matrix
with collections as columns across the nine programme rows, ending on the
battery footnote.

One deliberate divergence. The programme calls the third collection Gaming;
Novi's assortment calls it Power Gaming and uses that string as the join key
across the catalog, facets, badges and the A/B matrix. Renaming it would
touch the study. The record now carries the programme name separately and
this page uses it, so the page is faithful without moving the catalog.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
